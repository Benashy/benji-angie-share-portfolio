{
  "name": "benji-angie-share-portfolio",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs && python3 -m unittest discover -s tests -p 'test_*.py'",
    "check": "node --check app.js && node --check supabase/functions/_shared/portfolio-core.js && npm test"
  }
}
